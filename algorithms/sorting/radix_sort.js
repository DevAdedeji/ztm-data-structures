// O(n log n) means sorting by comparisons
// Non comparison sort
// Radix sort or counting sort

// Non comparison sorting uses the way that data is sorted.
// Counting sort and radix sort works with numbers in a specific range.

// Counting sort and radix

// Sometimes if numbers are within a certain it radix sort and counting sort are options