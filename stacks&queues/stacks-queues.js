// Stack and queues are very similar. They are both called linear data structures


// They allows us to traverse elements sequentially
// Unlike an array, theres no randomness. We're only dealing with items at the start and end of the data structure.